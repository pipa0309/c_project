#include <stdio.h>
#include <mat.h>

#define N 3
#define L 2

void scale(double* pVector, int size)
{
	int s = vector[N] * L;
}

void printVector()
{
	printf()
}

double setVector(double* pVector, double x, double y, double z)
{
	pVector[0] = 1;
	pVector[1] = 2;
	pVector[2] = 3;
}

double copyVector(double a, doble b)
{
	double tmp = 0;

}

int main()
{
	double vector1[N] = { 0 };
	double vector2[N] = { 0 };
	setVector(vector1, 1, 2, 3);
	copyVector(vector2, vector1);
	scale();
	printf(vector1);
	printf(vector2);
	return 0;
}